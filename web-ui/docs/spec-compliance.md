# oar-ui v0.2.3 Spec Compliance

Last updated: 2026-04-05

This document maps `docs/oar-ui-spec.md` requirements to current implementation,
known limitations, and test coverage.

## Integration Execution Notes

Backend (Terminal A):

```bash
cd ../core
./scripts/dev
```

UI integration test (Terminal B):

```bash
cd ../web-ui
OAR_WORKSPACES='[{"slug":"local","label":"Local","coreBaseUrl":"http://127.0.0.1:8000"}]' \
OAR_DEFAULT_WORKSPACE=local \
./scripts/e2e-with-core
```

This ticket validates behavior against a real `oar-core` instance via
`./scripts/e2e-with-core` as a complement to mocked Playwright and unit tests.

## Legend

- Status: `Met`, `Partial` (with explicit limitation), or `Deferred`
- Tests reference file paths under `tests/`

## 0. Purpose

| Requirement                                     | Status | Implementation                                                                                                                                                    | Known limitations                                                                                 | Tests                                                                                          |
| ----------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Operator-facing visibility + intervention UI    | Met    | Topics, boards, cards, docs, inbox, threads (backing inspection routes), artifacts, and shell surfaces in `src/routes/*/+page.svelte`                                                                                        | None                                                                                              | `tests/e2e/shell.spec.js`, `tests/e2e/threads.spec.js`, `tests/e2e/thread-detail.spec.js`      |
| Must not maintain independent organizational DB | Met    | UI state is local-only; persistence goes through API clients (`src/lib/oarCoreClient.js`) which wrap generated contract client artifacts from `/contracts/gen/ts` | Browser auth/actor storage is workspace-scoped, but persistent state still lives only in `oar-core` | `tests/unit/actorClient.integration.test.js`, `tests/e2e/integration-core-golden-path.spec.js` |
| Must not perform real-world side effects        | Met    | No integrations for external side effects in route/components                                                                                                     | None                                                                                              | N/A (code-path audit)                                                                          |
| Must not orchestrate agents                     | Met    | No agent orchestration subsystem in this repo                                                                                                                     | None                                                                                              | N/A (code-path audit)                                                                          |

## 1. Integration Contract

| Requirement                                                                             | Status | Implementation                                                                                                                                                                                                                                           | Known limitations                                                                           | Tests                                                                                                                                        |
| --------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1 Treat oar-core as source of truth                                                   | Met    | All reads/writes via `coreClient`/`oarCoreClient`; `src/hooks.server.js` resolves proxied traffic from the active `/:workspace/...` context and `OAR_WORKSPACES` catalog                                                                                 | No offline cache invalidation strategy beyond fresh fetches                                 | `tests/e2e/integration-core-golden-path.spec.js`                                                                                             |
| 1.2 Support schema primitives and typed conventions                                     | Met    | Topics, cards, boards, documents, artifacts, events, receipts, reviews supported in routes/forms                                                                                                                                                        | Backing-thread inspection routes are read-only; they are not a second organizational root alongside topics | `tests/e2e/*.spec.js`, `tests/unit/*Utils*.test.js`                                                                                          |
| 1.2 Support board primitives and workspace shape                                        | Met    | Board list/detail pages use `boards.list`, `boards.get`, and `boards.workspace` through `src/lib/oarCoreClient.js`; board detail reads the server-owned projection and keeps canonical membership/backing refs explicit versus derived summary/freshness | No drag-and-drop ordering in v1 by design                                                   | `tests/e2e/boards.spec.js`                                                                                                                   |
| 1.2 Handle unknown event/artifact types gracefully                                      | Met    | Unknown timeline event handling in `src/lib/timelineUtils.js`; unknown artifact kind warning + raw panels in `src/routes/artifacts/[artifactId]/+page.svelte`                                                                                            | None                                                                                        | `tests/e2e/headless-smoke.spec.js`, `tests/e2e/artifacts.spec.js`                                                                            |
| 1.2 Preserve/show unknown fields                                                        | Met    | Raw JSON panels via `UnknownObjectPanel` across major surfaces                                                                                                                                                                                           | Preservation is by patch omission for unknown fields, not explicit schema-aware merge in UI | `tests/unit/topicPatch.test.js`, `tests/e2e/headless-smoke.spec.js`                                                                         |
| 1.3 Parse and navigate typed refs                                                       | Met    | `src/lib/typedRefs.js`, `src/lib/refLinkModel.js`, `src/lib/components/RefLink.svelte`                                                                                                                                                                   | Unknown prefixes intentionally non-clickable raw text                                       | `tests/unit/typedRefs.test.js`, `tests/unit/refLinkModel.test.js`, `tests/unit/artifactRefs.test.js`                                         |
| 1.4 Actor identity required for writes                                                  | Met    | Actor gate + workspace switcher in `src/routes/+layout.svelte`; workspace-scoped actor/auth state in `src/lib/actorSession.js` and `src/lib/authSession.js`; `withActorId` in `oarCoreClient`                                                            | Actor selection remains browser-local, but isolated per workspace                           | `tests/e2e/shell.spec.js`, `tests/unit/actorSession.test.js`, `tests/unit/actorClient.integration.test.js`                                   |
| 1.4.1 Auth-first model; dev actor mode is development-only                              | Met    | `dev_actor_mode` from `/meta/handshake` gates actor gate/picker UI; unauthenticated users redirect to `/login` when `dev_actor_mode=false`; login page hides "Back to actor mode" link                                                                   | `dev_actor_mode=true` required for legacy actor picker/creator flow                         | Manual verification                                                                                                                          |
| 1.5 Provenance rendering + inferred distinction + by_field                              | Met    | `ProvenanceBadge` with inferred/evidence styling and `by_field` expansion                                                                                                                                                                                | `by_field` is shown as JSON details, not fully custom per-field UI widgets                  | `tests/unit/provenanceUtils.test.js`, `tests/e2e/receipts.spec.js`, `tests/e2e/reviews.spec.js`                                             |
| 1.6 Patch/merge semantics + list replacement + no core-maintained field writes           | Met    | Patch forms only emit editable fields; list fields wholesale replaced; core-maintained fields are read-only                                                                       | No dedicated visual diff preview before patch submit                                        | `tests/unit/topicPatch.test.js`, `tests/e2e/thread-detail.spec.js`                                                                          |
| 1.7 Follow reference conventions when creating events                                   | Met    | UI receipt/review payload construction includes deterministic refs (`artifact detail`, card-scoped subjects, `inbox`)                                                                                                                        | Convention enforcement ultimately server-side; UI validates only key ref fields             | `tests/e2e/receipts.spec.js`, `tests/e2e/reviews.spec.js`, `tests/e2e/integration-core-golden-path.spec.js` |

## 2. Core UX Model (Topic and board-first)

| Requirement                                                                    | Status | Implementation                                                                                   | Known limitations            | Tests                                                                  |
| ------------------------------------------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------ | ---------------------------- | ---------------------------------------------------------------------- |
| Topics and boards as primary navigation                                        | Met    | Sidebar + canonical topic/board routes at `src/routes/[workspace]/topics/*` and `src/routes/[workspace]/boards/*`; topic pages reuse the shared threads implementation internally | `/threads/*` URLs remain legacy-compatible backing-thread inspection paths, not a second primary product root | `tests/e2e/threads.spec.js`, `tests/e2e/boards.spec.js` |
| Detail shows current state + timeline distinction                               | Met    | Current-state section and timeline section in detail views                                       | None                         | `tests/e2e/thread-detail.spec.js`                                      |
| Timeline time-ordered/stable rendering + typed ref links + unknown type safety | Met    | Timeline transform in `toTimelineView`; per-event rendering with typed refs and unknown fallback | No custom icons per type yet | `tests/unit/timelineUtils.test.js`, `tests/e2e/headless-smoke.spec.js` |

## 3. Required UI Surfaces

| Requirement                                                   | Status | Implementation                                                                                                                                                                                                                                       | Known limitations                                                                   | Tests                                                                                                                          |
| ------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| Inbox grouping, navigation, ack, decision recording           | Met    | `src/routes/[workspace]/inbox/+page.svelte`, grouping utils in `src/lib/inboxUtils.js`                                                                                                                                                               | No ranking engine (expected v0 behavior)                                            | `tests/e2e/inbox.spec.js`, `tests/unit/inboxUtils.test.js`                                                                     |
| Topic list with filters and creation                                          | Met    | Canonical topic route at `src/routes/[workspace]/topics/+page.svelte`, backed by the shared implementation in `src/routes/[workspace]/threads/+page.svelte` and `src/lib/topicFilters.js`                                                        | No server-side pagination yet                                                       | `tests/e2e/threads.spec.js`, `tests/unit/topicFilters.test.js`                                                                |
| Detail edit + packets + timeline + message posting | Met    | Detail routes for topics/threads                                                                                                                                                                                             | Artifact linking is via editable typed refs, no dedicated picker | `tests/e2e/thread-detail.spec.js`, `tests/e2e/receipts.spec.js`, `tests/e2e/reviews.spec.js`                                                             |
| Board list and board detail mutation flows                    | Met    | `src/routes/[workspace]/boards/+page.svelte`, `src/routes/[workspace]/boards/[boardId]/+page.svelte`; common board flows use searchable thread/doc/owner pickers with manual-ID fallback behind advanced disclosures                                   | Explicit controls only; no drag-and-drop in v1                                      | `tests/e2e/boards.spec.js`                                                                                                     |
| Docs list/detail lineage presentation and linkage flows       | Met    | `src/routes/[workspace]/docs/+page.svelte`, `src/routes/[workspace]/docs/[documentId]/+page.svelte`; docs are presented as living lineages with head + revision history, and common thread linkage uses searchable pickers with advanced manual fallback | Structured/binary revision editing still requires CLI/API                           | `tests/e2e/docs.spec.js`                                                                                                       |
| Receipts/reviews from boards (no thread Work tab)            | Met    | Matches `oar-ui-spec.md` §3.4: card detail modals on boards for receipt/review authoring; thread/topic detail **Work** tab is not part of v0                                                                                                            | None                                                                                | `tests/e2e/receipts.spec.js`, `tests/e2e/reviews.spec.js`, `tests/e2e/boards.spec.js`                                                                           |
| Receipt viewer + intake + review action                       | Met    | Review workflow in artifact detail; receipt/review utilities for typed refs                                                                                                                                                                         | Review rubric intentionally lightweight per v0                                      | `tests/e2e/receipts.spec.js`, `tests/e2e/reviews.spec.js`, `tests/unit/receiptUtils.test.js`, `tests/unit/reviewUtils.test.js` |

## 4. Grounding and Restricted Updates

| Requirement                                                                    | Status  | Implementation                                                             | Known limitations                                                          | Tests                                                                                      |
| ------------------------------------------------------------------------------ | ------- | -------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| Restrict status transitions until evidence refs are provided                   | Met     | Validation blocks restricted updates until the required refs are present    | UI does not offer chooser for existing refs; manual entry remains possible | `tests/e2e/reviews.spec.js`                      |
| Show per-field provenance on restricted status updates                         | Met     | Status provenance panel on restricted state updates                          | Display is summary-oriented, not timeline-joined                           | `tests/e2e/reviews.spec.js`                                                            |
| Evidence affordances                                                           | Partial | Typed ref entry available across forms/events                                | No dedicated evidence graph yet        | `tests/e2e/receipts.spec.js`, `tests/e2e/reviews.spec.js` |

## 5. Messages

| Requirement                                      | Status | Implementation                                         | Known limitations                                              | Tests                                                                               |
| ------------------------------------------------ | ------ | ------------------------------------------------------ | -------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| Message posting as `message_posted` thread event | Met    | Message form in thread detail creates `message_posted` | None                                                           | `tests/e2e/thread-detail.spec.js`, `tests/e2e/integration-core-golden-path.spec.js` |
| Replies reference parent event via `event:<id>`  | Met    | Reply selector adds `event:<replyToEventId>` ref       | No nested threaded visual grouping (explicitly optional in v0) | `tests/e2e/thread-detail.spec.js`, `tests/e2e/integration-core-golden-path.spec.js` |

## 6. Concurrency

| Requirement                                      | Status  | Implementation                                                                                                                                                                                                                                                         | Known limitations                             | Tests                             |
| ------------------------------------------------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | --------------------------------- |
| Optimistic locking on current-state edits        | Met     | Sends `if_updated_at`; 409 reload warning path in current-state edit flow                                                                                                                                                                                            | No merge UI; operator re-applies changes manually | `tests/e2e/thread-detail.spec.js` |
| Optimistic locking on restricted state edits     | Met     | Sends `if_updated_at`; 409 shows warning and reloads latest state                                                                                                                                                                                                   | No side-by-side diff for conflict resolution  | `tests/e2e/reviews.spec.js`   |
| Optimistic locking on board and board-card edits | Met     | Board create/edit/card add-move-update-remove flows send `if_updated_at` / `if_board_updated_at`; conflict responses trigger a recoverable reload message and fresh workspace fetch; board detail also surfaces projection freshness so derived scan trust is explicit | No merge UI for stale board forms             | `tests/e2e/boards.spec.js`        |
| Poll/subscribe for external changes              | Partial | Manual refresh/reload behavior only                                                                                                                                                                                                                                    | No active polling or subscription channel yet | N/A (known limitation)            |

## 7. Extensibility

| Requirement                               | Status  | Implementation                                                        | Known limitations                                                     | Tests                                                                                                             |
| ----------------------------------------- | ------- | --------------------------------------------------------------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Unknown event types render safely         | Met     | Unknown event label + raw payload/refs in timeline                    | None                                                                  | `tests/e2e/headless-smoke.spec.js`                                                                                |
| Unknown artifact kinds render safely      | Met     | Unknown kind warning + raw metadata/content panels in artifact detail | No custom visualization for future artifact kinds (raw fallback only) | `tests/e2e/integration-core-golden-path.spec.js` (typed-ref nav), manual unknown-kind path via fallback component |
| Unknown fields preserved on round-trip    | Met     | Patch generation only includes editable known fields                  | Preservation depends on backend patch semantics (as intended by spec) | `tests/unit/topicPatch.test.js`                                            |
| Unknown ref prefixes rendered raw         | Met     | `resolveRefLink` unknown path returns non-link label                  | None                                                                  | `tests/unit/refLinkModel.test.js`, `tests/unit/inboxUtils.test.js`                                                |
| Unknown detail surfaces degrade gracefully | Met     | Topic/thread detail and raw JSON fallbacks across major routes       | None                                                                  | `tests/e2e/headless-smoke.spec.js`                                                                                 |

## 8. v0 Release Definition Checklist

| v0 release item                                      | Status | Evidence                                                                                                                                                                                       |
| ---------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Inbox grouped + ack + navigation                     | Met    | `src/routes/[workspace]/inbox/+page.svelte`, `tests/e2e/inbox.spec.js`                                                                                                                                     |
| List and filter topics                               | Met    | Canonical topic navigation under `src/routes/[workspace]/topics/*`, with shared topic detail/list implementation reused from `src/routes/[workspace]/threads/*`; covered by `tests/e2e/threads.spec.js`                                                                                                |
| Topic/detail editable current state + timeline refs   | Met    | Canonical topic detail route at `src/routes/[workspace]/topics/[topicId]/+page.svelte`, backed by the shared implementation in `src/routes/[workspace]/threads/[threadId]/+page.svelte`; `tests/e2e/thread-detail.spec.js`                                                                                                      |
| Restricted state updates with provenance              | Met    | Card/topic detail current-state sections and provenance display, `tests/e2e/reviews.spec.js`                                                                                           |
| Card-scoped receipts with typed refs                 | Met    | Core integration golden path creates board/card/receipt via API; UI lists and artifact detail, `tests/e2e/integration-core-golden-path.spec.js`                                                   |
| View receipts + typed evidence refs                  | Met    | Artifact detail receipt view, `tests/e2e/receipts.spec.js` (guidance), `tests/e2e/integration-core-golden-path.spec.js`                                                                          |
| Perform lightweight review                           | Met    | Artifact detail review form, `tests/e2e/reviews.spec.js`                                                                                                                                       |
| Post messages on thread                              | Met    | Thread detail message form, `tests/e2e/thread-detail.spec.js`                                                                                                                                  |
| Inferred vs evidence-backed provenance distinction   | Met    | `ProvenanceBadge`, `tests/unit/provenanceUtils.test.js`                                                                                                                                        |
| Parse and navigate typed refs across surfaces        | Met    | `typedRefs`/`refLinkModel`/`RefLink`, `tests/unit/refLinkModel.test.js`, `tests/e2e/artifacts.spec.js`                                                                                         |
| Parse and navigate `board:` refs across surfaces     | Met    | Same typed-ref pipeline plus board routes under `src/routes/[workspace]/boards/*`; verified by `tests/unit/typedRefs.test.js`, `tests/unit/refLinkModel.test.js`, and `tests/e2e/boards.spec.js` |
