#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
ARTIFACT_DIR="${SMOKE_ARTIFACT_DIR:-$ROOT_DIR/.tmp/e2e-smoke}"
REQUESTED_CORE_PORT="${CORE_PORT:-18080}"
REQUESTED_UI_PORT="${UI_PORT:-4179}"
AUTO_SELECT_PORTS="${AUTO_SELECT_PORTS:-1}"
CORE_HOST="127.0.0.1"
UI_HOST="127.0.0.1"
UI_WORKSPACE_SLUG="${UI_WORKSPACE_SLUG:-${UI_PROJECT_SLUG:-local}}"
SMOKE_TIMEOUT_SECONDS="${SMOKE_TIMEOUT_SECONDS:-25}"
BOOTSTRAP_TOKEN="${BOOTSTRAP_TOKEN:-smoke-bootstrap-token}"

mkdir -p "$ARTIFACT_DIR"
TMP_DIR="$(mktemp -d "$ARTIFACT_DIR/run.XXXXXX")"

CORE_LOG="$TMP_DIR/core.log"
UI_LOG="$TMP_DIR/web-ui.log"
EVENTS_TAIL_OUT="$TMP_DIR/events-tail.jsonl"
INBOX_TAIL_OUT="$TMP_DIR/inbox-tail.jsonl"

HOME_DIR="$TMP_DIR/home"
mkdir -p "$HOME_DIR"
CLI_BIN="$TMP_DIR/oar"
AGENT_NAME="smoke-agent"
USERNAME="smoke.agent"

cleanup() {
  local code=$?
  if [[ -n "${UI_PID:-}" ]] && kill -0 "$UI_PID" >/dev/null 2>&1; then
    kill "$UI_PID" >/dev/null 2>&1 || true
    wait "$UI_PID" >/dev/null 2>&1 || true
  fi
  if [[ -n "${CORE_PID:-}" ]] && kill -0 "$CORE_PID" >/dev/null 2>&1; then
    kill "$CORE_PID" >/dev/null 2>&1 || true
    wait "$CORE_PID" >/dev/null 2>&1 || true
  fi

  if [[ $code -ne 0 ]]; then
    echo "smoke failed; artifacts kept at: $TMP_DIR" >&2
    echo "core log: $CORE_LOG" >&2
    echo "web-ui log: $UI_LOG" >&2
  else
    echo "smoke artifacts: $TMP_DIR"
  fi
}
trap cleanup EXIT INT TERM

require_cmd() {
  local cmd="$1"
  command -v "$cmd" >/dev/null 2>&1 || {
    echo "required command not found: $cmd" >&2
    exit 1
  }
}

pick_free_port() {
  node -e '
const net = require("net");
const host = process.argv[1];
const server = net.createServer();
server.unref();
server.on("error", (err) => {
  console.error(err.message);
  process.exit(1);
});
server.listen(0, host, () => {
  const address = server.address();
  if (!address || typeof address.port !== "number") {
    console.error("failed to resolve a free port");
    process.exit(1);
  }
  process.stdout.write(String(address.port));
  server.close();
});
' "${1}"
}

resolve_port() {
  local requested_port="$1"
  local service_name="$2"

  if ! lsof -ti :"${requested_port}" >/dev/null 2>&1; then
    printf '%s\n' "${requested_port}"
    return 0
  fi

  if [[ "${AUTO_SELECT_PORTS}" != "1" ]]; then
    echo "port ${requested_port} is already in use; aborting to avoid killing unrelated processes" >&2
    lsof -i :"${requested_port}" >&2 || true
    exit 1
  fi

  local selected_port
  selected_port="$(pick_free_port "${CORE_HOST}")"
  echo "port ${requested_port} is already in use; using ${selected_port} for ${service_name}" >&2
  printf '%s\n' "${selected_port}"
}

wait_for_url() {
  local url="$1"
  local attempts="${2:-120}"
  local sleep_seconds="${3:-0.25}"
  shift 3 || true

  for _ in $(seq 1 "$attempts"); do
    if curl -fsS "$@" "$url" >/dev/null 2>&1; then
      return 0
    fi
    sleep "$sleep_seconds"
  done
  return 1
}

json_get() {
  local file="$1"
  local path="$2"
  node -e '
const fs = require("fs");
const file = process.argv[1];
const path = process.argv[2].split(".");
const input = fs.readFileSync(file, "utf8").trim();
if (!input) process.exit(2);
const json = JSON.parse(input);
let cur = json;
for (const key of path) {
  if (key === "") continue;
  if (cur === null || cur === undefined) {
    cur = undefined;
    break;
  }
  if (/^\d+$/.test(key)) {
    cur = cur[Number(key)];
  } else {
    cur = cur[key];
  }
}
if (cur === undefined || cur === null) process.exit(3);
if (typeof cur === "object") {
  process.stdout.write(JSON.stringify(cur));
} else {
  process.stdout.write(String(cur));
}
' "$file" "$path"
}

assert_contains() {
  local file="$1"
  local pattern="$2"
  if command -v rg >/dev/null 2>&1; then
    if rg -q "$pattern" "$file"; then
      return 0
    fi
  elif grep -Eq "$pattern" "$file"; then
    return 0
  fi

  echo "assertion failed: expected pattern '$pattern' in $file" >&2
  exit 1
}

wait_for_pid() {
  local pid="$1"
  local timeout_seconds="$2"
  local started
  started="$(date +%s)"

  while kill -0 "$pid" >/dev/null 2>&1; do
    local now
    now="$(date +%s)"
    if (( now - started >= timeout_seconds )); then
      return 1
    fi
    sleep 0.2
  done

  wait "$pid"
}

run_cli_json() {
  local outfile="$1"
  shift
  HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" "$@" >"$outfile"
}

board_create_attempt() {
  printf '{"board":{"id":"%s","title":"Smoke Board","status":"active","labels":["smoke","board"],"primary_thread_id":"%s","primary_document_id":"%s","pinned_refs":["thread:%s","artifact:%s"]}}\n' \
    "$BOARD_ID" "$THREAD_ID" "$PRIMARY_DOC_ID" "$THREAD_ID" "$WORK_ORDER_ID" \
    | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" boards create >"$TMP_DIR/board-create.json"
}

ensure_board_created() {
  local max_attempts="${1:-2}"
  local attempt error_code status_code

  for attempt in $(seq 1 "$max_attempts"); do
    if board_create_attempt; then
      return 0
    fi

    # GitHub-hosted runners occasionally surface a transient 500 here even
    # though the preceding authenticated board setup succeeded locally and in
    # repeated stress runs. Probe for the board first, then retry once before
    # failing the full smoke flow.
    if run_cli_json "$TMP_DIR/board-get.json" boards get --board-id "$BOARD_ID"; then
      return 0
    fi

    error_code="$(json_get "$TMP_DIR/board-create.json" 'error.code' || true)"
    status_code="$(json_get "$TMP_DIR/board-create.json" 'error.details.status' || true)"
    if [[ "$attempt" -lt "$max_attempts" && "$error_code" == "internal_error" && "$status_code" == "500" ]]; then
      sleep 0.5
      continue
    fi
    return 1
  done

  return 1
}

require_cmd curl
require_cmd go
require_cmd node
require_cmd pnpm

CORE_PORT="$(resolve_port "${REQUESTED_CORE_PORT}" "oar-core")"
UI_PORT="$(resolve_port "${REQUESTED_UI_PORT}" "web-ui")"
CORE_BASE_URL="http://${CORE_HOST}:${CORE_PORT}"
UI_BASE_URL="http://${UI_HOST}:${UI_PORT}"

# Build CLI binary once for deterministic smoke steps.
(
  cd "$ROOT_DIR/cli"
  go build -o "$CLI_BIN" ./cmd/oar
)

EXPECTED_VERSION="$("${ROOT_DIR}/scripts/read-version.sh")"

# Start core.
(
  cd "$ROOT_DIR/core"
  OAR_BOOTSTRAP_TOKEN="$BOOTSTRAP_TOKEN" go run ./cmd/oar-core \
    --listen-addr "${CORE_HOST}:${CORE_PORT}" \
    --workspace-root "$TMP_DIR/workspace" \
    --schema-path "$ROOT_DIR/contracts/oar-schema.yaml" \
    >"$CORE_LOG" 2>&1
) &
CORE_PID=$!

if ! wait_for_url "$CORE_BASE_URL/readyz" 160 0.15; then
  echo "core did not become healthy" >&2
  cat "$CORE_LOG" >&2 || true
  exit 1
fi

# Start web-ui against core and verify startup handshake route.
(
  cd "$ROOT_DIR"
  OAR_CORE_BASE_URL="$CORE_BASE_URL" pnpm -C web-ui exec vite dev --host "$UI_HOST" --port "$UI_PORT" >"$UI_LOG" 2>&1
) &
UI_PID=$!

if ! wait_for_url "$UI_BASE_URL/meta/handshake" 180 0.2 -H "x-oar-workspace-slug: ${UI_WORKSPACE_SLUG}"; then
  echo "web-ui did not become ready on /meta/handshake" >&2
  cat "$UI_LOG" >&2 || true
  exit 1
fi

# Verify UI serves shell too.
curl -fsS -D "$TMP_DIR/ui-shell-headers.txt" -o /dev/null "$UI_BASE_URL/"

# Handshake/version checks.
curl -fsS "$CORE_BASE_URL/meta/handshake" >"$TMP_DIR/core-handshake.json"
curl -fsS "$CORE_BASE_URL/version" >"$TMP_DIR/core-version.json"
assert_contains "$TMP_DIR/core-handshake.json" '"schema_version"'
assert_contains "$TMP_DIR/core-version.json" '"schema_version"'
if [[ "$(json_get "$TMP_DIR/core-handshake.json" 'core_version')" != "$EXPECTED_VERSION" ]]; then
  echo "core_version mismatch in handshake" >&2
  cat "$TMP_DIR/core-handshake.json" >&2 || true
  exit 1
fi
if [[ "$(json_get "$TMP_DIR/core-handshake.json" 'min_cli_version')" != "$EXPECTED_VERSION" ]]; then
  echo "min_cli_version mismatch in handshake" >&2
  cat "$TMP_DIR/core-handshake.json" >&2 || true
  exit 1
fi
if [[ "$(json_get "$TMP_DIR/core-handshake.json" 'recommended_cli_version')" != "$EXPECTED_VERSION" ]]; then
  echo "recommended_cli_version mismatch in handshake" >&2
  cat "$TMP_DIR/core-handshake.json" >&2 || true
  exit 1
fi

UI_VERSION_HEADER="$(
  awk -F': *' '
    BEGIN { IGNORECASE = 1 }
    tolower($1) == "x-oar-ui-version" {
      value = $2
      sub(/\r$/, "", value)
      print value
      exit
    }
  ' "$TMP_DIR/ui-shell-headers.txt"
)"
if [[ "$UI_VERSION_HEADER" != "$EXPECTED_VERSION" ]]; then
  echo "ui version header mismatch" >&2
  cat "$TMP_DIR/ui-shell-headers.txt" >&2 || true
  exit 1
fi

# Register + whoami.
run_cli_json "$TMP_DIR/register.json" auth register --username "$USERNAME" --bootstrap-token "$BOOTSTRAP_TOKEN"
run_cli_json "$TMP_DIR/whoami.json" auth whoami

# Explicit refresh token grant (API fallback) to exercise token lifecycle endpoint.
PROFILE_PATH="$HOME_DIR/.config/oar/profiles/${AGENT_NAME}.json"
REFRESH_TOKEN="$(node -e 'const fs=require("fs"); const p=JSON.parse(fs.readFileSync(process.argv[1],"utf8")); process.stdout.write(String(p.refresh_token||""));' "$PROFILE_PATH")"
if [[ -z "$REFRESH_TOKEN" ]]; then
  echo "missing refresh token in profile: $PROFILE_PATH" >&2
  exit 1
fi
printf '{"grant_type":"refresh_token","refresh_token":"%s"}\n' "$REFRESH_TOKEN" | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" api call --method POST --path /auth/token >"$TMP_DIR/token-refresh-explicit.json"
assert_contains "$TMP_DIR/token-refresh-explicit.json" '"access_token"'

# Force automatic refresh path by expiring local access token.
node -e '
const fs = require("fs");
const file = process.argv[1];
const profile = JSON.parse(fs.readFileSync(file, "utf8"));
profile.access_token_expires_at = "2000-01-01T00:00:00Z";
fs.writeFileSync(file, JSON.stringify(profile, null, 2) + "\n");
' "$PROFILE_PATH"

run_cli_json "$TMP_DIR/token-status-before.json" auth token-status
run_cli_json "$TMP_DIR/threads-list-before.json" threads list
run_cli_json "$TMP_DIR/token-status-after.json" auth token-status

NEEDS_REFRESH_BEFORE="$(json_get "$TMP_DIR/token-status-before.json" 'data.needs_refresh' || true)"
NEEDS_REFRESH_AFTER="$(json_get "$TMP_DIR/token-status-after.json" 'data.needs_refresh' || true)"
if [[ "$NEEDS_REFRESH_BEFORE" != "true" ]]; then
  echo "expected needs_refresh=true before auto-refresh, got: ${NEEDS_REFRESH_BEFORE:-<missing>}" >&2
  exit 1
fi
if [[ "$NEEDS_REFRESH_AFTER" != "false" ]]; then
  echo "expected needs_refresh=false after auto-refresh, got: ${NEEDS_REFRESH_AFTER:-<missing>}" >&2
  exit 1
fi

# Typed command flow: thread -> commitment -> packets.
printf '{"thread":{"title":"Smoke thread","type":"incident","status":"active","priority":"p1","tags":["smoke","cli"],"cadence":"daily","current_summary":"smoke in progress","next_actions":["verify"],"key_artifacts":[],"provenance":{"sources":["actor_statement:smoke"]}}}\n' \
  | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" threads create >"$TMP_DIR/thread-create.json"
THREAD_ID="$(json_get "$TMP_DIR/thread-create.json" 'data.thread.id' || true)"
if [[ -z "$THREAD_ID" ]]; then
  THREAD_ID="$(json_get "$TMP_DIR/thread-create.json" 'data.body.thread.id' || true)"
fi
if [[ -z "$THREAD_ID" ]]; then
  echo "failed to parse thread id" >&2
  cat "$TMP_DIR/thread-create.json" >&2 || true
  exit 1
fi

printf '{"patch":{"current_summary":"smoke updated"}}\n' \
  | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" threads update --thread-id "$THREAD_ID" >"$TMP_DIR/thread-update.json"

NOW_RFC3339="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"
printf '{"commitment":{"thread_id":"%s","title":"Smoke commitment","owner":"%s","due_at":"%s","status":"open","definition_of_done":["smoke complete"],"links":["url:https://example.com/smoke"],"provenance":{"sources":["actor_statement:smoke"]}}}\n' "$THREAD_ID" "$AGENT_NAME" "$NOW_RFC3339" \
  | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" commitments create >"$TMP_DIR/commitment-create.json"

WORK_ORDER_ID="artifact-work-order-smoke"
RECEIPT_ID="artifact-receipt-smoke"
REVIEW_ID="artifact-review-smoke"

printf '{"artifact":{"id":"%s","refs":["thread:%s"],"summary":"Smoke work order"},"packet":{"work_order_id":"%s","thread_id":"%s","objective":"Exercise e2e smoke","constraints":["headless"],"context_refs":["thread:%s"],"acceptance_criteria":["all checks pass"],"definition_of_done":["receipt and review"]}}\n' "$WORK_ORDER_ID" "$THREAD_ID" "$WORK_ORDER_ID" "$THREAD_ID" "$THREAD_ID" \
  | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" work-orders create >"$TMP_DIR/work-order-create.json"

printf '{"artifact":{"id":"%s","refs":["thread:%s","artifact:%s"],"summary":"Smoke receipt"},"packet":{"receipt_id":"%s","work_order_id":"%s","thread_id":"%s","outputs":["artifact:%s"],"verification_evidence":["url:https://example.com/evidence"],"changes_summary":"smoke complete","known_gaps":[]}}\n' "$RECEIPT_ID" "$THREAD_ID" "$WORK_ORDER_ID" "$RECEIPT_ID" "$WORK_ORDER_ID" "$THREAD_ID" "$WORK_ORDER_ID" \
  | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" receipts create >"$TMP_DIR/receipt-create.json"

printf '{"artifact":{"id":"%s","refs":["thread:%s","artifact:%s","artifact:%s"],"summary":"Smoke review"},"packet":{"review_id":"%s","work_order_id":"%s","receipt_id":"%s","outcome":"accept","notes":"accepted","evidence_refs":["artifact:%s"]}}\n' "$REVIEW_ID" "$THREAD_ID" "$WORK_ORDER_ID" "$RECEIPT_ID" "$REVIEW_ID" "$WORK_ORDER_ID" "$RECEIPT_ID" "$RECEIPT_ID" \
  | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" reviews create >"$TMP_DIR/review-create.json"

# Board smoke: create a board, add/reorder/move/remove cards, and verify the
# canonical workspace through both core and the UI proxy.
printf '{"thread":{"title":"Smoke board card A","type":"process","status":"active","priority":"p2","tags":["smoke","board"],"cadence":"weekly","current_summary":"board card A","next_actions":["verify board ordering"],"key_artifacts":[],"provenance":{"sources":["actor_statement:smoke"]}}}\n' \
  | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" threads create >"$TMP_DIR/board-thread-a-create.json"
BOARD_THREAD_A_ID="$(json_get "$TMP_DIR/board-thread-a-create.json" 'data.thread.id' || true)"
if [[ -z "$BOARD_THREAD_A_ID" ]]; then
  BOARD_THREAD_A_ID="$(json_get "$TMP_DIR/board-thread-a-create.json" 'data.body.thread.id' || true)"
fi
if [[ -z "$BOARD_THREAD_A_ID" ]]; then
  echo "failed to parse board thread A id" >&2
  cat "$TMP_DIR/board-thread-a-create.json" >&2 || true
  exit 1
fi

printf '{"thread":{"title":"Smoke board card B","type":"process","status":"active","priority":"p2","tags":["smoke","board"],"cadence":"weekly","current_summary":"board card B","next_actions":["verify board movement"],"key_artifacts":[],"provenance":{"sources":["actor_statement:smoke"]}}}\n' \
  | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" threads create >"$TMP_DIR/board-thread-b-create.json"
BOARD_THREAD_B_ID="$(json_get "$TMP_DIR/board-thread-b-create.json" 'data.thread.id' || true)"
if [[ -z "$BOARD_THREAD_B_ID" ]]; then
  BOARD_THREAD_B_ID="$(json_get "$TMP_DIR/board-thread-b-create.json" 'data.body.thread.id' || true)"
fi
if [[ -z "$BOARD_THREAD_B_ID" ]]; then
  echo "failed to parse board thread B id" >&2
  cat "$TMP_DIR/board-thread-b-create.json" >&2 || true
  exit 1
fi

PRIMARY_DOC_ID="doc-smoke-board-primary"
CARD_DOC_ID="doc-smoke-board-card"

printf '{"document":{"id":"%s","title":"Smoke Board Primary Doc","status":"active","labels":["smoke","board"],"thread_id":"%s"},"refs":["thread:%s"],"content":"Primary board document","content_type":"text"}\n' "$PRIMARY_DOC_ID" "$THREAD_ID" "$THREAD_ID" \
  | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" docs create >"$TMP_DIR/board-primary-doc-create.json"

printf '{"document":{"id":"%s","title":"Smoke Board Card Doc","status":"active","labels":["smoke","board"],"thread_id":"%s"},"refs":["thread:%s"],"content":"Pinned card document","content_type":"text"}\n' "$CARD_DOC_ID" "$BOARD_THREAD_A_ID" "$BOARD_THREAD_A_ID" \
  | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" docs create >"$TMP_DIR/board-card-doc-create.json"

BOARD_ID="board-smoke"
if ! ensure_board_created 2; then
  echo "failed to create board after retry" >&2
  cat "$TMP_DIR/board-create.json" >&2 || true
  exit 1
fi

BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-create.json" 'data.board.updated_at' || true)"
if [[ -z "$BOARD_UPDATED_AT" ]]; then
  BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-create.json" 'data.body.board.updated_at' || true)"
fi
if [[ -z "$BOARD_UPDATED_AT" ]]; then
  run_cli_json "$TMP_DIR/board-get.json" boards get --board-id "$BOARD_ID"
  BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-get.json" 'data.board.updated_at' || true)"
fi
if [[ -z "$BOARD_UPDATED_AT" ]]; then
  BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-get.json" 'data.body.board.updated_at' || true)"
fi
if [[ -z "$BOARD_UPDATED_AT" ]]; then
  echo "failed to parse board updated_at after create" >&2
  cat "$TMP_DIR/board-create.json" >&2 || true
  cat "$TMP_DIR/board-get.json" >&2 || true
  exit 1
fi

run_cli_json "$TMP_DIR/board-get.json" boards get --board-id "$BOARD_ID"

run_cli_json "$TMP_DIR/board-card-a-add.json" boards cards add --board-id "$BOARD_ID" --thread-id "$BOARD_THREAD_A_ID" --column backlog --pinned-document-id "$CARD_DOC_ID"
BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-a-add.json" 'data.board.updated_at' || true)"
if [[ -z "$BOARD_UPDATED_AT" ]]; then
  BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-a-add.json" 'data.body.board.updated_at' || true)"
fi

run_cli_json "$TMP_DIR/board-card-b-add.json" boards cards add --board-id "$BOARD_ID" --thread-id "$BOARD_THREAD_B_ID" --column backlog --after "$BOARD_THREAD_A_ID"
BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-b-add.json" 'data.board.updated_at' || true)"
if [[ -z "$BOARD_UPDATED_AT" ]]; then
  BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-b-add.json" 'data.body.board.updated_at' || true)"
fi

run_cli_json "$TMP_DIR/board-card-b-reorder.json" boards cards move --board-id "$BOARD_ID" --thread-id "$BOARD_THREAD_B_ID" --column backlog --if-board-updated-at "$BOARD_UPDATED_AT" --before "$BOARD_THREAD_A_ID"
BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-b-reorder.json" 'data.board.updated_at' || true)"
if [[ -z "$BOARD_UPDATED_AT" ]]; then
  BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-b-reorder.json" 'data.body.board.updated_at' || true)"
fi

run_cli_json "$TMP_DIR/board-card-a-update.json" boards cards update --board-id "$BOARD_ID" --thread-id "$BOARD_THREAD_A_ID" --if-board-updated-at "$BOARD_UPDATED_AT" --pinned-document-id "$PRIMARY_DOC_ID"
BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-a-update.json" 'data.board.updated_at' || true)"
if [[ -z "$BOARD_UPDATED_AT" ]]; then
  BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-a-update.json" 'data.body.board.updated_at' || true)"
fi

run_cli_json "$TMP_DIR/board-card-b-move.json" boards cards move --board-id "$BOARD_ID" --thread-id "$BOARD_THREAD_B_ID" --column review --if-board-updated-at "$BOARD_UPDATED_AT"
BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-b-move.json" 'data.board.updated_at' || true)"
if [[ -z "$BOARD_UPDATED_AT" ]]; then
  BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-b-move.json" 'data.body.board.updated_at' || true)"
fi

run_cli_json "$TMP_DIR/board-card-a-remove.json" boards cards remove --board-id "$BOARD_ID" --thread-id "$BOARD_THREAD_A_ID" --if-board-updated-at "$BOARD_UPDATED_AT"
BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-a-remove.json" 'data.board.updated_at' || true)"
if [[ -z "$BOARD_UPDATED_AT" ]]; then
  BOARD_UPDATED_AT="$(json_get "$TMP_DIR/board-card-a-remove.json" 'data.body.board.updated_at' || true)"
fi

run_cli_json "$TMP_DIR/board-workspace.json" boards workspace --board-id "$BOARD_ID"
assert_contains "$TMP_DIR/board-workspace.json" "\"board_id\"[[:space:]]*:[[:space:]]*\"$BOARD_ID\""
assert_contains "$TMP_DIR/board-workspace.json" "\"thread_id\"[[:space:]]*:[[:space:]]*\"$BOARD_THREAD_B_ID\""
assert_contains "$TMP_DIR/board-workspace.json" "\"column_key\"[[:space:]]*:[[:space:]]*\"review\""

ACCESS_TOKEN="$(node -e 'const fs=require("fs"); const p=JSON.parse(fs.readFileSync(process.argv[1],"utf8")); process.stdout.write(String(p.access_token||""));' "$PROFILE_PATH")"
curl -fsS -H "x-oar-workspace-slug: ${UI_WORKSPACE_SLUG}" -H "Authorization: Bearer $ACCESS_TOKEN" "$UI_BASE_URL/boards/${BOARD_ID}/workspace" >"$TMP_DIR/ui-board-workspace.json"
assert_contains "$TMP_DIR/ui-board-workspace.json" "\"board_id\"[[:space:]]*:[[:space:]]*\"$BOARD_ID\""

# Event stream smoke: tail one event and then emit an event.
HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" events tail --thread-id "$THREAD_ID" --max-events 1 --reconnect=false >"$EVENTS_TAIL_OUT" 2>&1 &
EVENTS_TAIL_PID=$!
sleep 0.8
printf '{"event":{"type":"message_posted","thread_id":"%s","summary":"smoke stream event","refs":["thread:%s"],"payload":{"body":"hello"},"provenance":{"sources":["actor_statement:smoke"]}}}\n' "$THREAD_ID" "$THREAD_ID" \
  | HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" events create >"$TMP_DIR/event-create.json"

if ! wait_for_pid "$EVENTS_TAIL_PID" "$SMOKE_TIMEOUT_SECONDS"; then
  kill "$EVENTS_TAIL_PID" >/dev/null 2>&1 || true
  wait "$EVENTS_TAIL_PID" >/dev/null 2>&1 || true
  echo "events tail did not complete in time" >&2
  exit 1
fi
assert_contains "$EVENTS_TAIL_OUT" '"command"[[:space:]]*:[[:space:]]*"events stream"'

# Ensure inbox has at least one item before stream tail.
for _ in $(seq 1 40); do
  run_cli_json "$TMP_DIR/inbox-list.json" inbox list
  INBOX_COUNT="$(node -e 'const fs=require("fs"); const j=JSON.parse(fs.readFileSync(process.argv[1],"utf8")); const items=j?.data?.items||j?.data?.body?.items||[]; process.stdout.write(String(items.length));' "$TMP_DIR/inbox-list.json")"
  if [[ "$INBOX_COUNT" -gt 0 ]]; then
    break
  fi
  sleep 0.3
done
if [[ "${INBOX_COUNT:-0}" -le 0 ]]; then
  echo "expected inbox items for stream smoke, found none" >&2
  cat "$TMP_DIR/inbox-list.json" >&2 || true
  exit 1
fi

LAST_INBOX_EVENT_ID="$(node -e '
const fs=require("fs");
const j=JSON.parse(fs.readFileSync(process.argv[1],"utf8"));
const items=j?.data?.items||j?.data?.body?.items||[];
const item=items[0];
if (!item || !item.id) process.exit(1);
const base=String(item.id);
process.stdout.write(base+"@bootstrap");
' "$TMP_DIR/inbox-list.json" 2>/dev/null || true)"

HOME="$HOME_DIR" "$CLI_BIN" --json --base-url "$CORE_BASE_URL" --agent "$AGENT_NAME" inbox tail --max-events 1 --reconnect=false >"$INBOX_TAIL_OUT" 2>&1 &
INBOX_TAIL_PID=$!
if ! wait_for_pid "$INBOX_TAIL_PID" "$SMOKE_TIMEOUT_SECONDS"; then
  kill "$INBOX_TAIL_PID" >/dev/null 2>&1 || true
  wait "$INBOX_TAIL_PID" >/dev/null 2>&1 || true
  echo "inbox tail did not complete in time" >&2
  exit 1
fi
assert_contains "$INBOX_TAIL_OUT" '"command"[[:space:]]*:[[:space:]]*"inbox stream"'

# Basic UI compatibility check after typed flow.
curl -fsS -H "x-oar-workspace-slug: ${UI_WORKSPACE_SLUG}" "$UI_BASE_URL/meta/handshake" >"$TMP_DIR/ui-handshake.json"
assert_contains "$TMP_DIR/ui-handshake.json" '"schema_version"'

cat >"$TMP_DIR/summary.txt" <<SUMMARY
core_base_url=${CORE_BASE_URL}
ui_base_url=${UI_BASE_URL}
thread_id=${THREAD_ID}
board_id=${BOARD_ID}
agent=${AGENT_NAME}
profile_path=${PROFILE_PATH}
artifacts_dir=${TMP_DIR}
SUMMARY

echo "e2e smoke completed successfully"
cat "$TMP_DIR/summary.txt"
